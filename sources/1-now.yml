plugin: servicenow.itsm.now
instance:
  host: https://dev109340.service-now.com
  # not used since injected as environment variables
  #username: "{{ sn_username }}"
  #password: "{{ sn_password }}"
group_by:
  os:
    includes:
      - Linux Red Hat
      - Windows XP
columns:
  - classification
  - cpu_type    
  - cpu_count
  - os_version
  - host_name
  - os
table: cmdb_ci_server 
ansible_host_source: name
